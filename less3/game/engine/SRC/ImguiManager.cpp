//
// Created by lilu on 15/12/2021.
//
/*
#include "ImguiManager.h"
#include <imgui.h>


ImguiManager::ImguiManager(const Engine &engine)
                :_engine(engine)
                , _eventHandler(&eventManager(), ext::genUniqueObjectId()) {
    IMGUI_CHECKVERSION();
    ImGui::CreateContext();
    ImGui::StyleColorsLight();
    ImGuiIO &io = ImGui::GetIO();
    io.BackendPlatformName = "custom_micro_engine";

    этот класс должен быть как спрайт, отрисовывать vertex и их будет сильно больше чем в спрайте
    работает почти как спрайт

    size_t offset =0;
    for(int cmd_i=0; cmd_i<cmd_list -> CmdBuffer.Size;cmd_i++)
    {
        const
    }


}

*/

