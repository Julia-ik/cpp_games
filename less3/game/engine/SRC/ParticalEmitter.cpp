//
// Created by lilu on 22/12/2021.
//
/*
#include "ParticalBuffer.h"
#include "ParticalEmitter.h"
#include <random>
ParticalEmitter::ParticalEmitter(const Engine &engine, size_t count)
{
    std::vector<ParticalBuffer::ParticalData> particles;
    particles.reserve(count);

    auto startPos = getTransform()* glm::vec3(_position, 1.0));
    
std::random_device rd;
std::mt19937 gen();
std::uniform_real_distribution<float>angleGen(0,360);
    std::uniform_real_distribution<float>velGen(0,360);


    for(size_t i; i<=count; i++)
    {

    }
}*/